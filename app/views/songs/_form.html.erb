<%= form_for @song do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>


    <%= f.label :artist_id %>
    <%= f.select :artist_id, Artist.pluck(:name, :id), :include_blank => true %>

    <%= f.submit %>
<% end %>